<source>
  type forward
</source>

<match **>
  type parser
  key_name log
  format json
  tag log
</match>

<match log>
  type elasticsearch
  format json
  logstash_format true
  host localhost
  port 9200
  index_name fluentd
  type_name fluentd
  include_tag_key true
</match>
